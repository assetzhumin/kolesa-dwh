[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: marks tests as integration tests (requires docker compose services)
    unit: marks tests as unit tests (no network/database required)

